# Core runtime. Apple Silicon: this stock onnxruntime wheel already includes
# the CoreML provider — nothing to swap. NVIDIA: replace onnxruntime with
# onnxruntime-gpu (see README "Performance"). numpy pinned <2 for insightface.
numpy<2
opencv-python>=4.8
insightface==0.7.3
onnxruntime>=1.16
pyvirtualcam>=0.11
